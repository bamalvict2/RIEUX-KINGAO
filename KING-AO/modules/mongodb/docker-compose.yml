version: "3.9"

services:
  mongodb:
    build: .
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db
      - ./logs:/var/log/mongodb
    networks:
      - kingao-net

networks:
  kingao-net:
    external: true
