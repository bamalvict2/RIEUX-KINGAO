version: "3.9"

services:
  portal:
    image: nginx:latest
    container_name: portal
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./portal-nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - kingao-net

networks:
  kingao-net:
    external: true
